#include <stdio.h>
int lastIndex(int arr[], int n, int x)
{
for(int i=n-1;i>=0;i--)
if(arr[i]==x) return i;
return 0;
}
int main()
{
int n,x;
scanf("%d",&n);
int a[n];
for(int i=0;i<n;i++)
scanf("%d",&a[i]);
scanf("%d",&x);
printf("%d",lastIndex(a,n,x));
return 0;
}
